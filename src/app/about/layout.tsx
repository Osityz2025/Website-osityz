export const metadata = {
  title: "About | Osityz",
  description:
    "Meet the team behind Osityz — two founders building the operating system for maritime commercial workflows.",
};

export default function AboutLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
